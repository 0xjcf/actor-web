# Persistent Decision Memory

[2026-05-30] **Workflow mode 6-agent** selected for "Add topology source factory for Actor-Web Ignite DX": Task includes architecture, platform, or shared-contract risk indicators..
[2026-05-29] **Actor-Web + Ignite target DX**: The locked consumer-facing model is `defineBehavior` builder -> `defineActorWebTopology` -> `igniteCore`. Do not add an Ignite-specific helper such as `igniteActorSource`; Ignite should consume Actor-Web source/source-handle shapes directly. Actor-Web should expose `topology.source("actorKey")` as the explicit topology-owned source factory, not a no-arg `topology.source()`, because topologies can have multiple actors/sources. The source factory must be lifecycle-safe for local/browser usage and preserve context/message/event inference from `defineBehavior` and `defineActorWebTopology`. (Builder renamed `defineActor` -> `defineBehavior` on 2026-06-09; `.build()` is now optional.)
[2026-04-30] **Commit discipline**: 2-step plan for "Logistics Docker Compose worker restart recovery verification". Steps: feat: implement logistics docker compose worker restart recovery verification; test: cover logistics docker compose worker restart recovery verification.
[2026-04-30] **Commit discipline**: 3-step plan for "Actor-Web docs consistency pass for current runtime guarantees and deployment roadmap". Steps: feat: implement actor-web docs consistency pass for current runtime guarantees and deployment roadmap; test: cover actor-web docs consistency pass for current runtime guarantees and deployment roadmap; chore: clean up actor-web docs consistency pass for current runtime guarantees and deployment roadmap.
[2026-04-30] **Commit discipline**: 3-step plan for "Make examples/fas-agent-loop use real Actor-Web runtime boundaries". Steps: feat: implement make examples/fas-agent-loop use real actor-web runtime boundaries; test: cover make examples/fas-agent-loop use real actor-web runtime boundaries; chore: clean up make examples/fas-agent-loop use real actor-web runtime boundaries.
[2026-04-30] **Memory-informed implementation**: 3 high-signal memory entries guided this task. Top entry from patterns.md: "[2026-04-30] **Script modifications for "Actor-Web docs consistency pass for current runtime guarantees and deployment roadmap"**."
[2026-04-30] **Workflow mode 6-agent** selected for "Actor-Web docs consistency pass for current runtime guarantees and deployment roadmap": Task includes architecture, platform, or shared-contract risk indicators..
[2026-04-23] **AI guidance (CLAUDE.md)**: 5 rules found. Key rules: - Default to `single-agent`.; - Escalate to `4-agent` or `6-agent` when risk or complexity increases.; - Optimize for low cognitive load, strong guardrails, and consistent execution..
[2026-04-23] **Lint command**: `pnpm lint`.
[2026-04-23] **Linting/formatting**: Uses Biome.
[2026-04-23] **Test command**: `pnpm test`.
[2026-04-23] **Testing**: Uses Vitest for testing.
[2026-04-23] **TypeScript**: TypeScript enabled with strict mode.

[2026-06-11T02:56:03Z] [spike:direct-1781143982247] **Spike capture (stigmergic-lattice-spike)**: Stigmergic lattice coordination should be an optional runtime extension shipped as a separate package/entry point (@actor-web/lattice or @actor-web/runtime/lattice) built only on public primitives — not a core feature, not an experimental branch. Rationale: no core runtime changes required; building it on defineBehavior + topology + emit/subscribe dogfoods the framework's extensibility; pre-1.0 core API should stay small. Full analysis: .fas/artifacts/stigmergic-lattice-spike/analysis.md. | Minimal primitives are Artifact (typed, versioned, append-only with latest-per-key head; immutable versions; content-hash idempotent re-publish) and Dependency (serializable conjunction of artifact matchers). Activation model: declarative dependsOn evaluated at both publish time and registration time (gives late-joiner and crash-recovery correctness); query-based when(ctx => ...) closures are rejected at topology level because they violate the locked declarative non-executable topology rule — queries remain an imperative ask escape hatch. Modes once vs everyVersion cover one-shot stages and rework loops. | Hybrid orchestrated+lattice execution needs no workflow({mode}) runtime switch: orchestrators are ordinary actors that publish artifacts, register dependencies (e.g. on a final approved artifact), and retain direct ask/send for transactional edges (kickoff, budget watchdog, human approval gates). Mode A/B/C walkthrough of Research→Planning→Coding→Review is in the analysis artifact.
