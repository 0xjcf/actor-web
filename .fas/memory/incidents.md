# Persistent Incident Memory

[2026-06-12] **CI test lane intermittently red: unhandled rejection in transport subscriber (PR #27)** — the new verify gate failed 2 of 3 runs in `headless-host.test.ts > fails closed when provider runtime is enabled but unavailable` with an UNHANDLED error (`Transport ... is not connected ... readyState=missing`), while local runs and one CI run passed. Root cause: every transport dispatches `subscribe` listeners without awaiting, and actor-system-impl's subscriber was async — when a runtime-protocol handler failed AND the error reply could not be sent (peer gone), the rejection escaped. Load-dependent: fast machines finish before the ask timeout fires. Fixed in PR #27 (contained at the subscription boundary + deterministic remote-transport unit test injecting a ghost-peer ask). Triage pattern that found it: diff the SHAs of consecutive green/red CI runs — identical code red+green = timing-dependent; then read the vitest "Unhandled Errors" section, not the test assertions, and trace who fires the rejecting promise without holding it. <!-- fas:meta v=1 cf=2026-06-12T00:00:00.000Z -->
[2026-06-12] **Claude Code subagent sandbox constraints in 6-agent runs (supervisor-trees task)**: (1) BACKGROUND delegated implementers are auto-denied Edit and compound Bash (`a && b`) — no interactive approval channel exists for background agents; first occurrence, separate investigation filed. Workaround: human-approved root takeover (`retry-context kind root-takeover, replacementReason explicit-takeover` — note replacementReason is an ENUM: failed-checkpoint|missing-handoff|scope-drift|repeated-bad-fixes|context-poisoning|explicit-takeover, prose goes in --notes). (2) Read-only subagents (QA/SRE/reviewer) are also denied vitest/pnpm-test AND git diff-family commands (show/diff/log) — they must verify via direct tree reads plus .fas/state/verification receipts (latest.json planAlignment + postReceiptChanges freshness is the evidence standard). Write delegation prompts accordingly: never instruct subagents to run tests or git show. (3) Every fas_senior_engineer retry start INVALIDATES downstream QA/SRE — bundle fix-round items (QA+SRE findings together) to minimize reconfirmation cycles; each reconfirm is a focused read-only pass citing the fix commit and current receipts. <!-- fas:meta v=1 cf=2026-06-12T00:00:00.000Z -->
[2026-06-11] **Closeout diff window includes other PRs after merge**: `fas done` after merging PR #20 failed NO_UNPLANNED_SOURCE_FILES on `actor-system-impl.ts` — a file the task never touched. Cause: `workflowStartHeadSha` is pinned at bootstrap (e34f81f), and post-merge closeout diffs start-SHA..HEAD on main, sweeping in every PR merged since bootstrap (here PR #18/#19). Workaround: set `workflowStartHeadSha` in `.fas/state/current-task.json` to the merge commit's first parent (main immediately before the PR merge) so the window covers only the task's own PR. FAS platform fix: post-merge closeout should derive the window from the PR's merge commit (first-parent diff), not bootstrap-time HEAD. Same drift family as the 2026-05-28 closeout classification incident. <!-- fas:meta v=1 cf=2026-06-11T00:00:00.000Z -->
[2026-06-11] **`fas spike pickup --start` handoff broken**: pickup resolves the next brief but invokes `start-fas-task.sh --brief <path>`, an option the script does not support ("Unknown option: --brief"), so implementation start fails after the pickup banner; pickup also printed a trailing-hyphen brief filename (`...-the-local-.md`) that does not exist on disk. Workaround: bootstrap manually with `.fas/scripts/start-fas-task.sh "<brief title>" --affected-files ...` and proceed through the normal pipeline. Treat as FAS platform drift — fix belongs in the platform repo (pickup→start handoff contract), not in project-local state. <!-- fas:meta v=1 cf=2026-06-11T00:00:00.000Z -->
[2026-06-11] **FAS tracker writer drops blank line before `## Template`**: every `fas scope refresh` (and task-entry rewrite) regenerates the active task block in `.fas/TASKS.md` without a trailing blank line, so the next `## Template` heading fails MD022/MD032 and `fas validate-task` lint:md fails on a file FAS itself wrote. Workaround: re-insert the blank line and commit before rerunning validate-task. FAS platform fix: tracker writer should emit a trailing blank line after the task block (same family as the spike-pickup bug filed in the FAS repo queue on 2026-06-11). <!-- fas:meta v=1 cf=2026-06-11T00:00:00.000Z -->
[2026-05-28] **FAS closeout classification churn**: During "Repair FAS workspace dependency and review artifact metadata", `fas validate-task`, `fas verify --full`, and delegated review all passed, but `fas done` stayed blocked because closeout-readiness classified approved `.fas` artifact edits as generated-only and reported `.fas/workspace-dependencies.json` as a missing planned implementation file despite the committed diff. Treat recurrence as FAS platform plan-alignment/classification drift; capture a FAS-side fix rather than hand-editing repo runtime state. <!-- fas:meta v=1 cf=2026-05-28T00:00:00.000Z -->
