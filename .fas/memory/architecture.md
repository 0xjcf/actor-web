# Persistent Architecture Memory

[2026-04-23] **Description**: > **Pure Actor Model for JavaScript/TypeScript** - Build resilient, distributed systems with location-transparent actors, inspired by Erlang/OTP.
[2026-04-23] **External dependencies** (Node): `@actor-core/testing`, `xstate`, `@biomejs/biome`, `@types/node`, `@vitest/coverage-v8`, `@xstate/graph`, `happy-dom`, `tsup`, `typescript`, `vite` (11 total).
[2026-04-23] **Module layout**: `docs/` (examples, spikes), `scripts/`, `src/` (testing), `tests/`.
[2026-04-23] **Project type**: Node.js project `@actor-web/core`.

[2026-06-11T02:56:03Z] [spike:direct-1781143982247] **Spike capture (stigmergic-lattice-spike)**: Indirect coordination today: behavior handler returns `{ emit: [...] }` (otp-types.ts:66-123) → OTPMessagePlanProcessor.processEmitArray (otp-message-plan-processor.ts:335-478) → emitEventToSubscribers (actor-system-impl.ts:2408-2463) → AutoPublishingRegistry.getSubscribersForEvent (auto-publishing.ts:268-314) → direct enqueue to each subscriber's bounded FIFO mailbox (messaging/mailbox.ts:69-297). Publisher never knows consumers; filtering is exact event-type match. | Declarative wiring: defineActorWebTopology accepts `subscriptions: [{ from, to, events }]` typed against actor keys (topology.ts:264, 520, 657); the local runtime client wires and re-establishes them on every start and tears down on stop (actor-web-client.ts:484-514). serveNode/startActorWebNode do NOT consume topology.subscriptions. | Failure path: on crash, mailbox destroyed and in-flight messages lost (actor-system-impl.ts:644-675); restart limits 3 per 30s with exponential backoff; restarted actor has fresh state; imperative subscriptions survive single-actor restart but not system restart. | Stigmergy = choreography + persistent environment. Actor-Web's emit/AutoPublishingRegistry/topology-subscriptions path already implements the choreography half (working precedent: examples/fas-agent-loop multi-agent topology, fas-topology.ts:20-127). The missing half is a persistent artifact medium with dependency-based activation; events are transient so late joiners and restarted actors miss coordination signals. | Transport delivery is at-most-once: SendInstruction modes 'retry(3)'/'guaranteed' are declared in message-plan.ts:72-79 but plan-interpreter.ts:270-305 treats all sends as fireAndForget. Any coordination layer needing reliable activation must implement ack + timeout re-emit + idempotent activation IDs at the protocol level, not assume transport guarantees.
