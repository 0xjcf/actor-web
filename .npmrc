# Wallaby.js configuration for pnpm
# These patterns allow Wallaby to dynamically load and patch dependencies
public-hoist-pattern[]=*vitest*
public-hoist-pattern[]=vite-node
public-hoist-pattern[]=@vitest/*
public-hoist-pattern[]=vite

# Additional patterns that may be needed for testing
public-hoist-pattern[]=happy-dom
public-hoist-pattern[]=@happy-dom/*

# Ensure peer dependencies are auto-installed
auto-install-peers=true